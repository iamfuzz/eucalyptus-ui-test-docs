Fetch Credentials
=================

This test does NOT use Selenium or directly access the Console UI.  The Console UI has issues when being used with the default 'eucalyptus' account, so the credentials for another account must be used; they are retrieved to the machine running the test suite via this test.
