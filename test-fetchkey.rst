Fetch Key
=========

This test is written in shell and does NOT use selenium or access the Console UI.  It simply creates and retrieves an SSH keypair to be used by ater tests to interact with spawned instances.
